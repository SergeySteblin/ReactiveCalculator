<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    android:background="@drawable/leaves_patterns_white_background"
    tools:context="com.sergey.apps.rxcalc.MainActivity">

    <TextView
        android:id="@+id/error_description"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:gravity="center"
        android:textSize="@dimen/app_description_textSize"
        android:text="@string/app_description"
        android:textStyle="bold"/>

    <include
        android:id="@+id/add_fragment_container"
        layout="@layout/operation_container"/>

    <include
        android:id="@+id/sub_fragment_container"
        layout="@layout/operation_container"/>

    <include
        android:id="@+id/mul_fragment_container"
        layout="@layout/operation_container"/>

    <include
        android:id="@+id/div_fragment_container"
        layout="@layout/operation_container"/>

</LinearLayout>
